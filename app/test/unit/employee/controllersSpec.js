/**
 * Created by serhii.holdun on 10/6/15.
 */

'use strict';

describe('hunt-coder.employee module', function() {

    beforeEach(module('hunt-coder.employee'));

    describe('employee controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var EmployeeListCtrl = $controller('EmployeeListCtrl');
            expect(EmployeeListCtrl).toBeDefined();
        }));

    });
});